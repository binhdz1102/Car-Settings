# Copy to keystore.properties and point it at a platform keystore.
# The debug/test setup uses the public AOSP development platform key; never
# commit the real file or a production key.
storeFile=keystore/platform.p12
storePassword=android
keyAlias=platform
keyPassword=android
storeType=PKCS12

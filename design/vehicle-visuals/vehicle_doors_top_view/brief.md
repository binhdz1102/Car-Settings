# Doors, windows, and mirrors top view

Create a 1600x900 layered vector vehicle outline. Required layers: `vehicle_body_base`, one layer
per door, one layer per window, left/right mirrors, and lock markers. Door layers rotate around
their hinge pivots; window layers move inside their masks; mirror fold has its own pivot. Movement
commands are explanatory only and must not be represented as observed position.
